server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/magobasti/Eureka-prueba.git
          searchPaths: config-data
          username: ${magobasti}
          password: ${Latano123}
          default-label: "master"
  security:
    user:
      name: root
      password: s3cr3t  